@model IEnumerable<Watch>

    <div class="container p-3">
        <div class="row pt-4">
            <div class="col-6">
                <h2 class="text-primary">Список часов</h2>
            </div>
        <div class="col-6 text-right">
            <a asp-action="Create" class="btn btn-primary"> Создать</a>
            </div>
        </div>

        <br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>Наименование</th>
                    <th>Цена</th>
                    <th>Категория</th>
                    <th>Вид</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td width="30%">@item.Name</td>
                        <td width="10%">@item.Price</td>
                        <td width="20%">@item.Category.Name</td>
                        <td width="20%">@item.ApplicationType.Name</td>
                        <td class="text-center">
                            <div class="w-50 d-flex justify-content-between" role="group">
                                <a asp-controller="Watch" asp-route-Id="@item.Id" asp-action="Edit" class="btn btn-primary mx-2">Редактировать</a>
                                <a asp-controller="Watch" asp-route-Id="@item.Id" asp-action="Delete" class="btn btn-danger mx-2">Удалить</a>

                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
            }    
        else
        {
            <p>По данному запросу ничего не найдено</p>
        }
    
</div>
